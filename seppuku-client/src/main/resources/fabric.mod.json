{
  "schemaVersion": 1,
  "id": "seppuku-client",
  "name": "Seppuku client",
  "version": "${version}",
  "environment": "client",
  "entrypoints": {},
  "mixins": [
    "seppuku-client.mixins.json"
  ],
  "depends": {
    "minecraft": "1.19.x",
    "java": ">=17",
    "seppuku-client-access": ">=${version}",
    "seppuku-client-components": ">=${version}"
  }
}